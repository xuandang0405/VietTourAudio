#!/usr/bin/env bash
set -euo pipefail

echo "Update this script with production domain, server path, systemd service, Nginx config and SSL steps before use."
echo "Suggested flow: pull code, build client, publish server, migrate database, reload services."
